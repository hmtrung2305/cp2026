#include <bits/stdc++.h>

constexpr int P = 1E9 + 7;

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int N, X, Y, Z;
    std::cin >> N >> X >> Y >> Z;

    std::vector<std::vector<int>> dp(A);
    for (int i = 0; i < N; i++) {

    }

    return 0;
}